const styles = theme => ({
    root: {
        background: '#f6f6f6',
        height: 'auto',
        minHeight: '100%',
    },
    loadingBar: {
        height: 2,
        position: 'absolute',
        backgroundColor: theme.palette.secondary.main,
    },
});

export default styles;
