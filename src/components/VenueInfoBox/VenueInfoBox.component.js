import React from 'react';
// import PropTypes from 'prop-types';

const VenueInfoBox = props => {
    console.log(props);
    return <div style={{ position: 'absolute', top: 0 }}>Test</div>;
};

export default VenueInfoBox;
