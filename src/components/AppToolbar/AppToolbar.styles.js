const styles = theme => ({
    logoIcon: {
        marginRight: 3 * theme.spacing.unit,
        color: theme.palette.primary.main,
    },
    logoText: {
        color: theme.palette.primary.main,
    },
});

export default styles;
